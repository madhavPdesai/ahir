$pipe  B: $uint<32> $depth 0
$pipe TMP: $uint<32> $depth 0
$module [stage_2_daemon] $in () $out () $is
{
	$branchblock[lOoP] {
		$merge $entry loopback $endmerge
			x := TMP
			B := x	
		$place [loopback]
	}
}
