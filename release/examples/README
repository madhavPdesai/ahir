you will need to have installed
	llvm-2.8
	clang-2.8
	ghdl


hello/  contains a simple example (with a README).
      try it first.


simple_processor/ is an example of a simple processor.


aa_pipeline/ is a "fast" pipeline written in Aa


matrix_multiply/ is a matrix multiplication system..  It
     uses four "processors" which compute dot products.

a5/ is the a5 stream cipher..


rng/ is a random-number-generator..

more will be added!

